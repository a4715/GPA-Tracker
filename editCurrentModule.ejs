<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>GPA Tracker</title>
  <style>
    body {
      background-color: #EDE7F6;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm" style="background-color: #3F51B5;">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">GPA Tracker</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/editCurrentModule">Edit Current Modules</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <br>
  <div class="container mt-4">
    <h2 class="text-center mb-4">Edit Current Module</h2>
    <br>
    <% if (Module) { %>
      <form action="/editCurrentModule/<%= Module.ModuleId %>" method="POST" enctype="multipart/form-data" class="mx-auto" style="max-width: 500px;">
        <div class="mb-3">
          <label for="name" class="form-label">Module Name</label><br>
          <input type="text" class="form-control" id="name" name="name" value="<%= Module.moduleName %>" required>
        </div>

        <div class="mb-3">
          <label for="code" class="form-label">Module Code</label><br>
          <input type="text" class="form-control" id="code" name="code" value="<%= Module.code %>" required>
        </div>

        <label>Add New Component</label>
        <div class="mb-3">
          <label for="assessment" class="form-label">Assessment</label>
          <input type="text" class="form-control" id="assessment" name="assessment" value="<%= Module.assessment %>" required>
        </div>

        <div class="mb-3">
          <label for="grade" class="form-label">Grade</label>
          <input type="text" class="form-control" id="grade" name="grade" value="<%= Module.grade %>" required>
        </div>

        <div class="mb-3">
          <label for="percentage" class="form-label">Weightage</label>
          <input type="number" class="form-control" id="Weightage" name="weightage" min="0" step="5" value="<%= Module.weightage %>" required>
            <div class="input-group-append"><span class="input-group-text">%</span>
          </div>
        </div>

        <table class="table table-bordered">
          <thead>
              <tr>
                  <th scope="col">Assessment</th>
                  <th scope="col">Grade</th>
                  <th scope="col">Weightage</th>
              </tr>
          </thead>
          <tbody>
             <% if (components && components.length > 0) { %> <!-- make sure it it does not repeat -->
              <% for (let i = 0; i < components.length; i++) { %>
              <tr>
                  <td><a href="/module/<%=modules[i].assessment%>"><%=modules[i].assessment%></a></td>
                  <td><a href="/module/<%=modules[i].grade%>"><%=modules[i].grade%></a></td>
                  <td><%= modules[i].weightage %>%</td>
              </tr>
              <% } %>
            <% } else { %>
              <tr>
                <td colspan="3" class="text-center">No components added yet.</td>
              </tr>
            <% } %>
          </tbody>
      </table>

        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-primary">Update</button>
          <a href="/moduleDetails" class="btn btn-secondary">Back to Module Details</a>
        </div>
      </form>
    <% } else { %>
      <p class="text-center">No module found.</p>
    <% } %>
  </div>
</body>
</html>


<!-- add delete to the table and have different button to update the component -->
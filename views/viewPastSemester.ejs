<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>GPA Tracker</title>
</head>
<body style="background-color: #ede7f6">
    <nav class="navbar navbar-expand-sm" style="background-color: #3f51b5;">
      <div class="container-fluid">
        <a class="navbar-brand" href="/" style="color:white"><b>GPA Tracker</b></a>
      </div>
    </nav> <br>

    <br>
    <div style="display: flex;">
    <% if (pastsemester.length > 0) { %>
    <h6 style="padding-left: 20px;">Semester: <%= pastsemester[0].semester %></h6>
    <% } else { %>
      <h6 style="padding-left: 20px;">Semester: <%= semesterId %></h6>
      <p style="padding-left: 20px;">No modules in this semester yet.</p>
    <% } %>
    <!-- Edit Link -->
    <a class="btn" style="margin-left: 55rem; background-color: #3f51b5; color:white" type="button" href="/editPastSemester">Edit</a>
    </div>
    </br>


    <h5 style="padding-left: 20px;">Modules:</h5>
    
    
    <div class="row" style="padding-left: 20px;">
    <div class="col-auto">
    <table class="table small text-center" style="min-width: 1000px;">
      <thead>
          <tr>
            <th>Module Name</th>
            <th>Module Code</th>
            <th>Final Grade</th>
            <th>Module Credits</th>
            <th></th>
      </thead>
      <tbody>
         <% if (pastsemester) { %> <!-- Check if student data exists -->
            <% for(let i=0; i < pastsemester.length; i++) { %>
              <tr>
                <td><%= pastsemester[i].module%></td>
                <td><%= pastsemester[i].code%></td>
                <td><%= pastsemester[i].grade%></td>
                <td><%= pastsemester[i].credits%></td>
                <!-- Delete Link -->
                <td><a class="btn btn-sm" type="button" style="background-color: #3f51b5; color:white"  href="/deletePastModule/<%= pastsemester[i].moduleId %>" onclick="return confirm('Are you sure you want to delete this module?')">Delete</a></td>
              </tr>
            <% } %>
        <% } else { %>
            <p>No past semester found.</p>
        <% } %>
      </tbody>
    </table>
    </div>
    </div> 
  
    
    

  
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>GPA Tracker</title>
</head>
<body style="background-color: #ede7f6">
    <nav class="navbar navbar-expand-sm" style="background-color: #3f51b5;">
      <div class="container-fluid">
        <a class="navbar-brand" href="/" style="color:white"><b>GPA Tracker</b></a>
      </div>
    </nav> <br>

    <br>
    <!-- Container to align the semester title and Edit button in a row -->
<div style="display: flex;">

    <!-- If there are modules in this semester, show the semester name from the first module -->
    <% if (pastsemester.length > 0) { %>
        <h6 style="padding-left: 20px;">Semester: <%= pastsemester[0].semester %></h6>
    <% } else { %>
        <!-- If there are no modules, show fallback semester ID and message -->
        <h6 style="padding-left: 20px;">Semester: <%= semesterId %></h6>
        <p style="padding-left: 20px;">No modules in this semester yet.</p>
    <% } %>

    <!-- Edit button that navigates to the edit page of this semester -->
    <a class="btn" 
       style="margin-left: 55rem; background-color: #3f51b5; color:white" 
       type="button" 
       href="/editPastSemester/<%= pastsemester[0].semesterId %>">Edit</a>
</div>
</br>

<!-- Title for the modules table -->
<h5 style="padding-left: 20px;">Modules:</h5>

<!-- Bootstrap row and column for layout -->
<div class="row" style="padding-left: 20px;">
  <div class="col-auto">

    <!-- Module data table -->
    <table class="table small text-center" style="min-width: 1000px;">
      <thead>
          <tr>
            <th>Module Name</th>     <!-- Header: name of the module -->
            <th>Module Code</th>     <!-- Header: code e.g. CS1010 -->
            <th>Final Grade</th>     <!-- Header: grade received -->
            <th>Module Credits</th>  <!-- Header: how many credits -->
            <th></th>                <!-- Empty column for delete button -->
          </tr>
      </thead>

      <tbody>
        <!-- Check if data exists before rendering -->
        <% if (pastsemester) { %>
            <!-- Loop through each module entry -->
            <% for(let i = 0; i < pastsemester.length; i++) { %>
              <tr>
                <!-- Display module data from each row -->
                <td><%= pastsemester[i].module %></td>
                <td><%= pastsemester[i].code %></td>
                <td><%= pastsemester[i].grade %></td>
                <td><%= pastsemester[i].credits %></td>

                <!-- Delete button for this module. Confirmation prompt will appear. -->
                <td>
                  <a class="btn btn-sm" 
                     type="button" 
                     style="background-color: #3f51b5; color:white"  
                     href="/deletePastModule/<%= pastsemester[i].moduleId %>" 
                     onclick="return confirm('Are you sure you want to delete this module?')">
                    Delete
                  </a>
                </td>
              </tr>
            <% } %>
        <% } else { %>
            <!-- If no data found, display this -->
            <p>No past semester found.</p>
        <% } %>
      </tbody>
    </table>

  </div>
</div>
  
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Module</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      background-color: #EDE7F6;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm" style="background-color: #3F51B5;">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="/">GPA Tracker</a>
    </div>
  </nav>

  <div class="container mt-5">
    <h2 class="text-center mb-4">Edit Current Module</h2>

    <% if (modules) { %>
    <form action="/editCurrentModule/<%= modules.module_id %>" method="POST">
      <input type="hidden" name="module_id" value="<%= modules.module_id %>">

      <div class="mb-3">
        <label for="module_name" class="form-label">Module Name</label>
        <input type="text" class="form-control" id="module_name" name="module_name" value="<%= modules.module_name %>" required>
      </div>

      <div class="mb-3">
        <label for="module_code" class="form-label">Module Code</label>
        <input type="text" class="form-control" id="module_code" name="module_code" value="<%= modules.module_code %>" required>
      </div>

      <hr>
      <h5>Current Components</h5>

      <table class="table table-bordered mt-3">
        <thead class="table-light">
          <tr>
            <th>Assessment</th>
            <th>Grade</th>
            <th>Weightage (%)</th>
          </tr>
        </thead>
        <tbody>
          <% if (components && components.length > 0) { %>
            <% for (let i = 0; i < components.length; i++) { %>
              <tr>
                <td>
                  <input type="hidden" name="component_id_<%= i %>" value="<%= components[i].id %>">
                  <input type="text" class="form-control" name="component_name_<%= i %>" value="<%= components[i].component_name %>" required>
                </td>
                <td>
                  <input type="text" class="form-control" name="grade_<%= i %>" value="<%= components[i].grade %>" required>
                </td>
                <td>
                  <input type="number" class="form-control" name="weightage_<%= i %>" value="<%= components[i].weightage %>" min="0" step="5" required>
                </td>
              </tr>
            <% } %>
          <% } else { %>
            <tr>
              <td colspan="3" class="text-center">No components found for this module.</td>
            </tr>
          <% } %>
        </tbody>
      </table>

      <button type="submit" class="btn btn-primary mt-3">Save Module Info</button>
      <a href="/moduleDetails/<%= modules.module_id %>" class="btn btn-secondary mt-3 ms-2">Back to Module Details</a>
    </form>
    <% } else { %>
      <p class="text-center">No module found.</p>
    <% } %>
  </div>
</body>
</html>
